from Math import Maths
from physics import Physics



response1 = input('Math(m) or Physics(p)')
if response.lower() == 'm':
    maths = maths ()
    maths.areaoftriangle()
    maths.areaofrectangle()
    maths.areaofasquare()
    maths.areaofcircle()
    maths.peremiterofrectangle()

    if response.lower() == 'p':
        physics.power()
        physics.force()
        physics.acceleration()
        physics.velocity()
        physics.mass()